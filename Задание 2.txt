Получить множество точек от пользователя и записать в 2 одинаковых списка
Проверить четное ли количество (если нет, сообщить об этом пользователю)

//Проверка на наличие прямой, параллельной оси y
Взять первую точку из списка и проверить, если ещё точки в списке с таким же значением "у"
Если их не четное количество, то прямой, параллельной оси y нет. Нужно проверить на наличие прямой, параллельной оси х.

Если точки с одинаковым значением "у" - 2:
	найти точку посередине между ними. у уже известен, х найти по формуле (х1+х2)/2
	записать точку в переменную

Если точек 4, 6 и т.д. 
	найти в списке точку с максимальным и  минимальным значнием х и найти точку посередине по логике выше
	и записать точку к переменную

после проверки первой пары точек, удалить их координаты из списка

Берем следущую точку из списка и прогоняем по той же логике
Если точек с таким же значением "у" не окажется, то прямой, параллельной оси у нет. Нужно проверить на наличие прямой, параллельной оси х.

Если точек четное количество, необходимо опять находить точку посередине между ними.
В данном случае полученную точку не записываем в переменную, а сравниваем с той, что уже записана в переменную при предыдущей проверке.
если значения "х" у этих точек окажутся одинаковыми, то перейти к проверке следующей пары точек по логике выше (предварительно удалив из списка уже проверенные)
Если значения "х" не совпадут, то прямой, параллельной оси y нет. Нужно проверить на наличие прямой, параллельной оси х.

Таким образом сравнивать пары точек, пока не окажется либо:
	что точки середины окажутся разными и прямой, параллельной оси y нет
	что все точки середины окажуются на одной прямой и можно вывести YES







						Если проверка на наличие прямой, параллельной оси у провалится, 

Взять первую точку из второго списка и проверить, если ещё точки в списке с таким же значением "х"
Если их не четное количество, то прямой, параллельной оси х нет. Выводить NO

Если точки с одинаковым значением "х" - 2:
	найти точку посередине между ними. х уже известен, у найти по формуле (у1+у2)/2
	записать точку в переменную

Если точек 4, 6 и т.д. 
	найти в списке точку с максимальным и  минимальным значнием у и найти точку посередине по логике выше
	и записать точку к переменную

после проверки первой пары точек, удалить их координаты из списка

Берем следущую точку из списка и прогоняем по той же логике
Если точек с таким же значением "х" не окажется, то прямой, параллельной оси х нет. Выводить NO

Если точек четное количество, необходимо опять находить точку посередине между ними.
В данном случае полученную точку не записываем в переменную, а сравниваем с той, что уже записана в переменную при предыдущей проверке.
если значения "у" у этих точек окажутся одинаковыми, то перейти к проверке следующей пары точек по логике выше (предварительно удалив из списка уже проверенные)
Если значения "у" не совпадут, то прямой, параллельной оси х нет. Выводить NO

Если все значения "у" совпадут, вывести YES